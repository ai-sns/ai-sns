[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Unit tests that do not require a running backend
    api: API tests that require a running backend on localhost:8788
    slow: Long-running stress or concurrency tests
